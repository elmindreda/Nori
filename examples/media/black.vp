
struct Out
{
  float4 position : POSITION;
};

Out main(uniform float4x4 MVP, in float3 position, in float3 normal)
{
  Out result;
  result.position = mul(MVP, float4(position, 1)) + float4(0, 0, 0.001, 0);
  return result;
}

